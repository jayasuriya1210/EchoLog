version: "3.9"

services:
  mongodb:
    image: mongo:7
    container_name: shiftlog-mongodb
    restart: always
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_ROOT_USERNAME: admin
      MONGO_INITDB_ROOT_PASSWORD: strongpassword
    volumes:
      - mongo-data:/data/db

volumes:
  mongo-data: